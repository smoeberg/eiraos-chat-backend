[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = src tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    f2_02_enforcement: F2-02 usage and budget enforcement acceptance tests
