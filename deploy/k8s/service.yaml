apiVersion: v1
kind: Service
metadata:
  name: eiraos-chat-backend-svc
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: eiraos-chat-backend
